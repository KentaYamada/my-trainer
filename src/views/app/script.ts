import Vue from "vue";
import Navigation from "@/components/navigations/Navigation.vue";

/**
 * App entry view
 */
export default Vue.extend({
  name: "app-view",
  components: {
    Navigation
  }
});
